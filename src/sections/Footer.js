import React from 'react';
import styled from 'styled-components';

const Footer = () => {
  return <FooterArea>Hello</FooterArea>;
};

const FooterArea = styled.section`
  height: 300px;
  background-color: var(--clr-footer-bcg);
`;

export default Footer;
