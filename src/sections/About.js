import React from 'react';
import styled from 'styled-components';

const About = () => {
  return <Wrapper id='about'>This is about page</Wrapper>;
};

const Wrapper = styled.section`
  height: 100vh;
`;

export default About;
