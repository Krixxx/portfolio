import React from 'react';
import styled from 'styled-components';

const Projects = () => {
  return <Wrapper id='projects'>This is Project section</Wrapper>;
};

const Wrapper = styled.section`
  height: 100vh;
`;

export default Projects;
